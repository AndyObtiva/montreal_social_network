<h2><%= feed_title %></h2>
<% if feed_posts.any? %>
  <ul>
    <% feed_posts.order(created_at: :desc).each do |post| %>
      <li><%= post.created_at %> - <%= post.feed.user.email %>: <%= post.text %></li>
    <% end %>
  </ul>
<% else %>
  <p>No posts</p>
<% end %>
